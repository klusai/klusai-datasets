# Virtual environment
.venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
htmlcov/
.coverage

# Secrets
.env

# Generated / downloaded data — published to HF, never committed.
# Anchored to repo root with a leading slash so they DON'T match the package dir
# klusai/privacy/datasets/data/ (which holds source, not generated data).
/artifacts/
/data/
*.jsonl
*.log

# OS
.DS_Store

# Local review artifact
review.html
